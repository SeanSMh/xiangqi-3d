# 象棋规则档案与提示词

本文是本项目的规则与中文提示词基线。实现、测试、AI 搜索、棋谱回放和界面文案都应以本文为准。

## 1. 规则档案

| 字段 | 值 |
| --- | --- |
| `ruleProfile` | `program-competition-2023` |
| 裁决方式 | 网页端自动裁决，无需棋手申请或人工裁判介入 |
| 主要依据 | 皮卡鱼开源社区《象棋程序竞赛规则》，修订日期 2023-11-22 |
| 适用范围 | 本地双人、人机对弈、AI 搜索、棋谱与回放 |
| 非目标 | 不宣称完整复刻中国象棋协会 2020 版线下人工裁判棋例 |

本档案选择可确定执行、可自动测试的程序规则。若未来增加“中国象棋协会 2020”或“世界象棋规则”模式，必须使用新的 `ruleProfile`，不得在同一档案内静默改变循环或限着判法。

本文中“一着”指一方走一步棋；“一回合”指红黑双方各走一着，即两着。

## 2. 基础行棋规则

- 棋盘为九路十线、共九十个交叉点；红方先行，双方轮流各走一着。
- 帅/将：在己方九宫内沿横线或竖线走一格。
- 仕/士：在己方九宫内沿斜线走一格。
- 相/象：斜走两格，不得越过河界；象眼有子时不能走。
- 马：先直走一格再斜走一格；马腿有子时不能走。
- 车：沿横线或竖线走任意格，不得越子。
- 炮：不吃子时与车相同；吃子时，炮与目标之间必须恰有一个炮架。
- 兵/卒：未过河时只可向前一格；过河后还可横走一格；任何时候都不能后退。
- 不得走到己方棋子占据的点；落到对方棋子所在点即吃子。
- 帅与将不得在同一路上无遮挡照面。
- 任何使己方帅/将仍受攻击或新陷入受攻击状态的着法均为非法着法。
- 一方被将军时，下一着必须解除将军。
- 引擎不以“吃掉帅/将”结束棋局；帅/将不能被实际捕获，终局应在将死或困毙时判定。

## 3. 盘面终局

每一着合法棋提交后，按以下顺序裁决；前面的结果优先，后续规则不再执行：

1. **将死**：当前行棋方正被将军且没有任何合法着法，当前方负，对方胜。
2. **困毙**：当前行棋方未被将军但没有任何合法着法，当前方负，对方胜。中国象棋的困毙不是和棋。
3. **三次同形循环**：按第 4、5 节裁决胜负或和棋。
4. **自然限着**：达到 120 个有效未吃子着，判和。
5. **无进攻子力**：盘面只剩帅/将、仕/士、相/象，判和。

协议和棋、认输、计时超时属于比赛控制层，不属于本阶段规则引擎的自动盘面裁决。

## 4. 三次同形

### 4.1 局面键

“同一局面”由以下内容共同确定：

- 所有未被吃棋子的颜色、种类和坐标完全相同；
- 当前轮到行棋的一方相同。

局面键不包含棋子 `id`、棋子数组顺序、已被吃棋子、历史着法、动画状态、选中状态、AI 状态或界面状态。同类同色棋子即使内部 `id` 不同，只要占据相同坐标，也属于同一盘面。

初始局面必须作为第一个局面键写入规则历史。每次合法着提交并完成盘面终局检测后，再记录新局面键。

### 4.2 触发与循环区间

同一局面在一局棋中第三次出现时，自动触发循环裁决。循环区间为该局面第一次出现至第三次出现之间的闭区间；区间外着法不参与本次违规等级判断。

本项目不要求玩家提出裁决，也不把所有三次同形一律判和。

## 5. 长将、长捉与循环等级

对循环区间内红黑双方分别分类，再比较违规等级：

| 分类 | 等级 | 判定摘要 |
| --- | ---: | --- |
| 长将 | `2` | 该方在循环区间内自己的每一着均为将军 |
| 长捉 | `1` | 循环区间内不存在将军着；存在同一枚对方棋子，该方每一着均捉该子，且对方每一着均使该子逃捉 |
| 允许循环 | `0` | 该方既非长将也非长捉 |

裁决规则：

- 双方等级相同：和棋。
- 双方等级不同：等级较高的一方判负，另一方获胜。
- 因此，单方长将对允许循环时长将方负；单方长捉对允许循环时长捉方负；长将对长捉时长将方负；双方长将、双方长捉或双方均允许循环时判和。

### 5.1 “捉”的程序判定边界

“攻击到目标”不等于“捉”。长捉判定必须使用合法吃子与逃捉关系，并按 `program-competition-2023` 排除至少以下情况：

- 攻击子吃掉目标后会被对方合法反吃；但马或炮攻击车、仕/士或相/象攻击马炮车的价值例外按程序规则处理。
- 攻击子是帅/将或兵/卒。
- 被攻击子是未过河兵/卒。
- 同类棋子相互攻击，且攻击子也处在目标子的攻击中。
- 循环中追逐的不是同一枚目标棋子。
- 对方某一着没有使该目标从“被捉”转为“不被捉”。

不得仅凭通用 attack map、棋子距离或连续移动方向判定长捉。无法可靠分类为长捉的循环应归入允许循环，避免无依据判负。

## 6. 自然限着

- 从一段连续不吃子的第一着开始累计“有效未吃子着”。
- 累计达到 `120` 着，即 60 回合时，自动判和。
- 任意一方完成吃子后，有效未吃子着数、双方将军计数和待排除的应将状态全部归零。
- 每方在同一段未吃子过程中，最多有 10 个将军着计入有效未吃子着。
- 某方第 11 次及之后的将军着不计数，由该将军直接引起的对手应将着也不计数。
- 达到第 120 个有效着时若同时形成将死、困毙或三次同形，应按第 3 节优先级裁决，不得先判自然限着和棋。

规则状态必须跟随棋局时间线保存。悔棋应恢复到对应历史状态；棋谱回放只读取状态，不得重复累计；重开应完全清零。

## 7. 无进攻子力和棋

盘面上双方均不再拥有车、马、炮、兵/卒，只剩帅/将、仕/士、相/象时，双方都不可能完成将死，自动判和。

本阶段不扩展其他经验性“官和”残局。任何新增死局模板都必须先有可证明条件和独立测试，不能只依据通常胜率或子力分数判和。

## 8. 与中国象棋协会 2020 版的差异

中国象棋协会 2020 版是当前国内线下竞赛规则，但其待判局面依赖棋手申请、裁判审核和大量棋例分类。本项目有意采用下列不同的自动化口径：

| 事项 | 本项目 `program-competition-2023` | 中国象棋协会 2020 版线下规则 |
| --- | --- | --- |
| 裁决发起 | 条件满足后自动裁决 | 通常由棋手提出，裁判审核；特定情况下裁判可介入 |
| 循环触发 | 同一局面第三次出现 | 双方着法循环反复达三次形成重复局面；循环首着、首着吃子等另有细则 |
| 循环分类 | 长将 `2`、长捉 `1`、允许循环 `0` | 还区分将、杀、捉、兑、献、拦、跟、闲、有根/无根/少根、联合捉、自毙等 |
| 变着机会 | 第三次同形时直接比较等级并结束 | 单方长将可立即判负；其他待判局面通常允许指定一方在两个回合内变着 |
| 开局提和 | 不实现人工提和限制 | 25 回合内原则上双方不得提和，并有红方变着等特殊条款 |
| 自然限着 | 120 个有效未吃子着，双方将军各最多计 10 着，自动判和 | 连续 60 回合未吃子，可按比赛等级酌减；由提出方申请，提出方将军最多计 10 着并由裁判审核 |

因此，界面和文档只能表述为“采用适合网页自动裁决的程序棋规”，不得写成“完整实现中国象棋协会 2020 版裁判规则”。

## 9. 结构化提示词

规则层返回结构化 `GameOutcome.reason`；界面统一通过 `deriveGamePrompt()` 把规则事实、交互结果、AI、回放和弹窗状态转成 `GamePrompt`。HUD 与 `render_game_to_text()` 共用该结果，不从自由文本反推规则状态。

当前稳定结构：

```ts
interface GamePrompt {
  code: GamePromptCode
  tone: 'info' | 'warning' | 'danger' | 'success'
  title: string
  detail?: string
  action?: string
  secondary?: string
}
```

| `code` | `tone` | 生产条件 | 标题／含义 |
| --- | --- | --- | --- |
| `result-checkmate` | `success` | 将死 | `{winner}胜 · 将死` |
| `result-stalemate` | `success` | 困毙 | `{winner}胜 · 困毙` |
| `result-repetition-draw` | `info` | 双方循环等级相同 | `和棋 · 循环局面`；详情区分双方允许循环、双方长将或双方长捉 |
| `result-perpetual-check` | `success` | 长将方等级更高并判负 | `{winner}胜 · 长将违规` |
| `result-perpetual-chase` | `success` | 长捉方等级更高并判负 | `{winner}胜 · 长捉违规` |
| `result-no-capture-limit` | `info` | 达到 120 个有效未吃子着 | `和棋 · 自然限着` |
| `result-bare-defenders` | `info` | 仅剩将士象 | `和棋 · 死局` |
| `must-answer-check` | `danger` | 当前方被将，或候选着未能应将 | `{side}被将军`／`这步没有解除将军` |
| `repetition-warning` | `warning` | 当前局面第二次出现 | `循环警告`；再次重复将触发循环裁决 |
| `natural-limit-progress` | `info` | 有效未吃子着为 100–109 | 显示 `{count}/120` |
| `natural-limit-warning` | `warning` | 有效未吃子着为 110–119 | 显示剩余 `{remaining}` 着 |
| `piece-selected` | `info` / `warning` | 已选择棋子 | 显示完整棋子名和合法落点数；规则预警放入 `secondary` |
| `wrong-side`、`no-selection`、`friendly-occupied`、`outside-board` | `warning` | 选择或落点错误 | 给出可执行的下一步提示 |
| `illegal-pattern`、`path-blocked`、`horse-leg-blocked`、`elephant-eye-blocked`、`elephant-cross-river`、`palace-bound`、`cannon-screen`、`pawn-direction` | `warning` | 棋子几何或阻挡规则不满足 | 说明马腿、象眼、炮架、九宫等具体原因 |
| `exposes-own-king`、`kings-facing` | `danger` | 着后送将或将帅照面 | 明确指出王安全错误 |
| `animation`、`ai-animation`、`ai-thinking`、`ai-turn`、`ai-paused-history`、`ai-error` | `info` / `warning` / `danger` | 演出或 AI 状态 | 解释锁盘、暂停和恢复方式 |
| `replay-playing`、`replay-view`、`match-settings`、`rule-help` | `info` | 回放或模态弹窗 | 说明棋盘只读及返回方式 |
| `turn-human`、`turn-local` | `info` | 正常待操作状态 | 说明当前行棋方与基本操作 |

`result-draw` 与 `result-adjudicated` 仅用于缺失细分原因的旧快照兼容回退。`terminal`、`locked-animation`、`locked-ai`、`locked-replay`、`locked-settings` 是点击被锁棋盘时的短时交互反馈。

第二次同形只显示中性 `repetition-warning`，不提前猜测责任方；第三次同形提交后才由规则引擎确定长将、长捉或允许循环。吃子会立即把结构化自然限着计数归零，但不额外弹出覆盖行棋提示的 toast。

预警只帮助玩家理解规则，不改变着法合法性。最终结果必须由规则层根据已提交局面裁决，不能由提示弹窗或动画回调决定。

## 10. 实现与验收约束

- 本地双人和人机模式必须共用同一个规则裁决入口。
- AI 搜索必须携带足够的局面键、循环分类和限着上下文；不得因搜索中清空历史而选择立即规则判负的着法。
- 规则判断必须是纯逻辑，不能依赖动画时钟、DOM、Web Worker 返回顺序或帧率。
- 将死/困毙、循环、自然限着和死局的终局结果必须可悔棋、可回放、可确定性复现。
- 至少覆盖：局面键与棋子数组顺序无关、行棋方不同则 key 不同；双方允许循环和棋；单方长将判负；单方长捉同一目标判负；追逐不同目标不误判；第 119/120 有效着边界；吃子清零；第 11 次将军及对应应将不计；第 120 着同时将死时将死优先；只剩将士象和棋；AI 不选择立即规则判负着。

## 11. 依据与延伸阅读

- [皮卡鱼开源社区：《中国象棋程序竞赛规则》（2023-11-22）](https://www.pikafish.com/rule.html)：本项目自动裁判档案的直接依据。
- [国家体育总局棋牌运动管理中心：2026 年赛事仍执行《象棋竞赛规则（2020版）》](https://www.sport.gov.cn/qpzx/n5384/c29447384/content.html)：确认国内正式竞赛当前采用的规则版本。
- [《象棋竞赛规则（2020版）》在线目录](https://cnchess.net/rules/rules2020.html)：中国象棋协会审定规则的网页转录，便于查阅胜负和、待判局面与棋例条款。
- [世界象棋联合会：《世界象棋规则》官方入口](https://www.wxf-xiangqi.org/index.php?Itemid=290&id=271&lang=cn&option=com_content&view=article)：世界规则中文 PDF 与制定背景。
- [世界象棋联合会：World Xiangqi Rules 官方英文入口](https://www.wxf-xiangqi.org/index.php?Itemid=291&catid=133&id=269%3Aworld-xiangqi-rules-introductionen&lang=en&layout=default&option=com_content&view=article)：世界规则英文 PDF。
