Original prompt: 那你设计开发方案，开始开发吧，合理利用资源

## 当前里程碑

- [x] 完整基础象棋规则引擎与测试
- [x] 本地双人点选、合法落点、走棋与吃子
- [x] 将军、绝杀、困毙与重开
- [x] `render_game_to_text` / `advanceTime` / 全屏验收钩子
- [x] production v3 红黑 14 张全彩角色、Alpha 合成、剪影回退与角色 billboard
- [x] 坐标棋谱、单步悔棋、逐手定位与确定性自动回放
- [x] Playwright 可玩验证与截图检查
- [x] `program-competition-2023` 三次同形、长将／长捉、自然限着与死局自动裁决
- [x] 结构化非法着原因、统一中文提示与内置规则说明
- [x] 鼠标／触控水平环绕、阵营正背面与棋盘文字随观战半场切换

## 约束

- 规则状态是唯一真相，Three.js 仅负责呈现。
- 红黑分别使用 production v3 定稿角色卡，并以同源 Alpha 裁切；颜色失败回退到锁定剪影，Alpha 未就绪或失败时使用几何占位，后续可按棋种逐步替换为 GLB。
- 美术资源只从 `public/assets/` 读取，禁止使用 `archive_*`。

## 2026-08-04 首个可玩里程碑

- `src/engine/moves.ts` 已实现帅/将、仕/士、相/象、马、车、炮、兵/卒，包含九宫、河界、象眼、马腿、炮架、将帅照面、自将过滤、将军、绝杀和困毙判负。
- `src/game/controller.ts` 负责选择、切换选择、合法着、走棋、吃子与重开；规则状态保持为唯一真相。
- `src/scene/boardScene.ts` 已接入 production 红黑底座 PNG，以及金色选择圈、白色合法圈、红色吃子圈；补全楚河断线和双方九宫线。
- HUD 已显示行棋方、将军、战果、终局、选择提示，并提供“重开 R”和“全屏 F”。
- 浏览器验收钩子已提供：`window.render_game_to_text()` 与 `window.advanceTime(ms)`。

## 验收记录

- `npm test`：2 个测试文件、19 个测试全部通过。
- `npm run typecheck`：通过。
- `npm run build`：通过，Vite 生产包已生成。
- Playwright：验证初始局面、红兵选择与合法圈、红黑各一步、红色吃子圈、兵吃卒、重开、全屏；最终吃子状态为 `ply=3`、`captured=["black:卒"]`，无控制台错误。

## 下一阶段 TODO

- [x] 增加 `AnimationDirector`：规则先结算，表现层锁输入并播放移动/受击/退场，完成后 `snapTo(nextState)`。
- [x] 完成车直线移动、阵营拖尾与通用吃子退场垂直切片。
- [x] 实现炮弹道和马日字跃迁，补齐三类演出差异。
- [x] 把 production v3 共用棋种剪影接到底座上；红黑共用造型，仅切换材质与阵营色。
- [x] 增加坐标棋谱、单步悔棋、逐手 / 自动回放，并支持悔棋后改走新分支。
- [x] 以红黑 14 张 production v3 定稿替换主视图剪影，并保留锁定剪影作为加载失败回退。
- [ ] 按棋种逐步试点 GLB 角色；继续遵守 `LOCKED_FEATURES.md`，不回退到错位或归档资源。
- [x] 增加玩家执红、AI 执黑的三级人机模式，并让 AI 搜索携带循环与限着上下文。
- [x] 完成第三次同形、长将／长捉责任比较、自然限着和仅剩将士象和棋。
- [x] 完成统一提示解析、具体非法着解释、第二次同形／自然限着预警和规则说明弹窗。
- [ ] 完成竖屏相机适配与窄屏专项截图验收。
- [ ] 竖屏阶段同步提供 512 / 768 运行时纹理与阴影质量分级，降低高分辨率角色卡在移动设备上的显存和阴影开销。

## 2026-08-04 第二阶段：走子与吃子演出

- 第一阶段已提交：`bd18e37 feat: 完成中国象棋本地双人可玩基线`。
- 新增 `src/animation/animationDirector.ts`，使用确定性 `travel → impact → victim-exit → settle` 状态机；权威局面先提交，动画期间只锁表现层输入。
- `BoardScene` 新增棋子浮点姿态、车阵营色拖尾、白青冲击核、橙金爆炸、受害者缩小抬升旋转，以及结束后的权威局面吸附。
- `window.advanceTime(ms)` 现在真实推进动画；`render_game_to_text()` 增加 `inputLocked`、`manualClock` 和完整动画快照。
- 重开可在任意动画阶段执行 `cancelAndSnap`；被吃 Mesh 删除时释放 geometry/material，缓存 Texture 保留复用。
- 修复过期 `Piece` 引用可能按错误坐标生成着法的问题，并补齐帅受攻击、被将应对、炮架将军和马腿将军测试。

### 第二阶段验收

- `npm test`：3 个测试文件、27 个测试全部通过。
- Playwright 半程：红车 `(0,0) → (0,1)` 在 `elapsedMs=183.333` 时视觉位置 `rank=0.568`，`inputLocked=true`；动画期间点击黑炮未产生新着法。
- Playwright 吃子：5 ply 合法序列触发红车吃黑炮，已截取 `impact` 与 `victim-exit`；最终 `captured=["red:马","black:炮"]`、动画回到 `idle`。
- Playwright 动画中重开：恢复 `ply=0`、32 子、`inputLocked=false`；全部场景无控制台错误。

## 2026-08-04 第三阶段：炮弹道与马跃迁

- 第二阶段已提交：`79bf8b9 feat: 增加走子与吃子演出状态机`。
- `AnimationDirector` 新增 `standard / chariot / horse / cannon` 演出类型；状态快照增加 `style` 与可观测的 `projectile` 姿态。
- 马使用固定 `480ms` 的日字贝塞尔路线：以马腿方向的相邻格作为控制点，并按曲线切线转向；半程最高抬升 `0.48` 格距。
- 炮吃子时炮身留在起点发射，炮弹以确定性抛物线飞向目标；命中后复用白青冲击与橙金爆炸，炮身在受害者退场阶段滑入目标格。
- `BoardScene` 新增炮弹亮核、橙色外焰和锥形短尾迹；结束或重开统一由 `clearTransientEffects()` 清理。
- 红黑双方共用棋种逻辑与特效参数，不依赖阵营专属贴图。

### 第三阶段验收

- `npm test`：3 个测试文件、29 个测试全部通过；新增马分步确定性、炮弹半程、命中和炮身入位断言。
- `npm run typecheck`：通过。
- `npm run build`：通过；仅保留 Three.js 单包超过 `500 kB` 的非阻断提示。
- Playwright 马跃半程：`elapsedMs=250`，视觉位置 `(1.271, 1.042)`、抬升 `0.479`、切线朝向 `0.48rad`；最终落在 `(2,2)` 并回到 `idle`。
- Playwright 炮弹半程：`elapsedMs=266.667`，炮身仍在 `(1,2)`，炮弹进度 `0.498`、位置 `(1,5.489)`、抬升 `1.25`。
- Playwright 炮击退场：`elapsedMs=833.333`，炮身已滑至 `rank=5.367`，受害者缩至 `0.256`，橙金爆炸生效。
- Playwright 最终局面：红炮完成 `(1,2) × (1,9)`，`captured=["black:马"]`、动画回到 `idle`；所有场景无控制台错误。

## 2026-08-04 第四阶段：production v3 角色剪影接入

- 第三阶段已提交：`7ceefd6 feat: 增加炮弹道与马跃迁演出`。
- 已核对 `LOCKED_FEATURES.md` 与 production/public 校验和：运行时只使用 7 张 `sil_*_alpha.png`；14 张红黑 JPG 保留为定稿与建模参考，全部 `archive_*` 继续禁用。
- 剪影 PNG 的 RGB 为纯黑、轮廓位于 Alpha 通道，因此不能直接依赖 `map × color`；`BoardScene` 改由材质 Shader 读取 `texture.a` 输出阵营色。
- 新增 `src/scene/pieceVisuals.ts`：集中保存七棋种资源 URL、Alpha 边界、落脚锚点和目标身高；可见横向占位全部不超过 `0.85` 格。
- 红黑共用同一棋种形状：红方暗红主体 / 金边，黑方玄青主体 / 冷蓝边；竖直角色卡采用 Y 轴 billboard，脚底锚定现有文字底座。
- `render_game_to_text()` 新增 `presentation` 摘要，可观察逻辑存活数、实际渲染实例、七类资源加载/失败状态。

### 第四阶段验收

- `npm test`：4 个测试文件、32 个测试全部通过。
- `npm run typecheck`：通过。
- `npm run build`：通过；仅保留 Three.js 单包超过 `500 kB` 的非阻断提示。
- Playwright 初局：32 个逻辑棋子对应 32 个渲染实例，红黑各 16，七类剪影全部 `ready`，无失败资源。
- Playwright 选中与马跃：选中圈、合法落点和角色卡均正常；马跃半程角色与底座同步沿日字轨迹移动。
- Playwright 炮击：炮弹飞行和受害者退场期间，权威局面已为 31 子、表现层仍保留 32 个实例；演出结束后收敛为 31 / 31，黑方 15 子。
- Playwright 演出中重开：立即恢复 `ply=0`、逻辑 / 渲染 32 / 32、动画 `idle`；全部场景无控制台错误。

## 2026-08-04 第五阶段：棋谱、悔棋与回放基础

- 第四阶段已提交：`452687a feat: 接入 production v3 棋子剪影`。
- 新增 `GameTimeline`：每个已提交着法保存稳定局面快照，实时分支与只读回放游标分离；悔棋只移除最后一个实时快照，不手写棋子逆操作。
- `GameController` 已提供逐手回放、任意已知 ply 定位、返回实时、单步悔棋及结构化坐标棋谱；显示文本使用 1-based 坐标，回放状态禁止棋盘继续落子并清空旧选择。
- 新增桌面棋谱抽屉与底栏控制：开局、上一步、播放 / 暂停、下一步、返回当前；`U / H / ArrowLeft / ArrowRight / Home / End / Space / Enter` 提供对应快捷键。
- 自动回放复用 `advanceSimulation()`，固定 `700ms / ply`，因此 `window.advanceTime()` 可确定性推进，不引入独立计时器。

### 第五阶段验收

- `npm test`：5 个测试文件、38 个测试全部通过，已覆盖普通着、吃子复活、绝杀恢复、回放只读、外部修改隔离和基线重置。
- `npm run typecheck`：通过。
- `npm run build`：通过；JS 产物 `528.76 kB`，仅保留单包超过 `500 kB` 的非阻断提示。
- Playwright 已验证：两步棋谱、回放锁盘、返回实时、悔棋后替代分支、自动回放 / 暂停及播放中重开；普通移动、炮弹飞行、受害者退场和吃子完成后悔棋均正确恢复。
- 吃子完成时为逻辑 / 渲染 `31 / 31`，悔棋后恢复 `32 / 32`；目前所有第五阶段场景均无控制台错误。
- 吃子棋谱回退到开局时恢复 `32 / 32`，再前进一步重新收敛为实时 `31 / 31`。
- 本阶段浏览器验收视口为 `1280×720`；窄屏样式已设断点，但竖屏相机适配与专项截图留到后续响应式阶段。

## 2026-08-04 第六阶段：AI 对弈与难度分级

- 第五阶段已提交：`1e99545 feat: 增加棋谱悔棋与确定性回放`。
- 保留本地双人，并新增“玩家执红、AI 执黑”的入门 / 标准 / 挑战三级人机模式；原生对局设置弹窗只在“应用并重开”后切换配置。
- AI 搜索运行在 Vite module Worker：搜索树使用 `generateAllLegalMoves()`、短路终局探测与轻量局面转换，最终候选仍经 `GameController → GameTimeline → applyMove()` 权威二次校验。
- 入门难度从固定种子的前四候选中选择，标准难度做一层稳定评估，挑战难度预判对方一手并受 `8,000` 节点上限约束；相同局面、难度与种子得到相同结果。
- `GameTimeline` 新增单调 `revision`；AI 结果必须同时匹配请求 ID 与 revision。悔棋、重开、回放、模式切换或替代分支都会终止 Worker，陈旧结果不能落盘。
- 人机悔棋原子回到玩家决策点：AI 思考时撤玩家 1 手，AI 已回复或动画中撤完整 2 手；AI 落子继续复用现有炮弹、马跃、移动与吃子演出。

### 第六阶段验收

- `npm test`：7 个测试文件、52 个测试全部通过；覆盖三级 AI 确定性、合法性、被将应对、两层战术避险、一步绝杀、困毙、节点预算与整轮降级、Worker 失败恢复 / revision 失效和整回合悔棋。
- `npm run typecheck` 与 `npm run build`：通过；Worker 独立产物 `7.00 kB`，主包 `540.73 kB`，仅保留超过 `500 kB` 的非阻断提示。
- Playwright 已验证：设置弹窗、三档完整回合、玩家走子 → AI 思考 → AI 吃子动画 → 返回玩家、思考中锁盘、思考 / 动画 / 回复后悔棋、AI 棋谱标识、回放期忽略悔棋且不插入 AI，以及切回本地双人。
- 入门难度完整回合为 `ply=2`、逻辑 / 渲染 `32 / 32`；标准难度完整回合为 `ply=2`、逻辑 / 渲染 `31 / 31`；挑战难度开局响应完成深度 2、`2,008` 个搜索节点。动画中与 AI 回复后的原子悔棋均恢复 `ply=0`、`32 / 32`，并清空 AI 决策；所有有效第六阶段场景均无控制台错误。
- 本阶段继续以桌面 `1280×720` 为完整视觉验收视口；竞赛循环规则已在下一阶段补齐。

## 2026-08-04 第七阶段：程序棋规与统一提示

- 新增 `RULES_AND_PROMPTS.md`，锁定 `program-competition-2023` 自动裁判档案，并明确与中国象棋协会 2020 版线下人工裁判规则的边界。
- 新增纯逻辑裁定层：局面键忽略棋子 `id` 和数组顺序但包含行棋方；同一局面第三次出现时比较长将 `2`、长捉 `1`、允许循环 `0`，等级高的一方判负，等级相同判和。
- 长捉使用合法吃子与逃捉关系，处理反吃保护、同类互捉、未过河兵、帅兵排除、马炮捉车和士象捉马炮车等程序棋规边界，不把普通攻击图直接当成长捉。
- 新增 120 个有效未吃子着自然限着：双方各自第 11 次及之后的将军与直接应将不计，吃子后循环窗口、有效着数和将军计数全部清零。
- 将死／困毙优先于循环，循环优先于自然限着，自然限着优先于仅剩将士象；所有结果和规则轨迹都进入 `GameTimeline`，可悔棋、可回放、可重开。
- AI 搜索复用权威规则转换并携带规则历史，能够正确评分规则和棋／判负，且不会选择立即令己方长捉判负的第三次同形着。
- 新增 `explainIllegalMove()`，可区分选错阵营、马腿、象眼、过河、九宫、阻挡、炮架、兵卒方向、未应将、送将和将帅照面。
- 新增 `deriveGamePrompt()` 作为 HUD 与 `render_game_to_text()` 的共同提示源；覆盖终局、AI、回放、弹窗、循环／限着预警和 21 类交互原因，并提供 `?` 规则说明入口。

### 第七阶段逻辑与构建验收

- `npm test`：10 个测试文件、127 个测试全部通过。
- `npm run typecheck`：通过。
- `npm run build`：通过；Worker `12.66 kB`，主包 `561.08 kB`（gzip `147.12 kB`），仅保留既有的单包超过 `500 kB` 非阻断提示。
- Playwright 真实点击已验证：规则说明打开时 `inputLockedReason=rule-help`、棋盘只读，关闭后恢复；点黑方棋子显示 `wrong-side`；红马点击被占马腿方向显示 `horse-leg-blocked`；提示在 `1800ms` 后恢复正常；合法走子进入 `ply=1` 且自然限着计数为 `1`。
- 已人工检查规则说明、选错阵营、蹩马腿、提示恢复、合法走子与关闭弹窗 6 张截图；布局和文案无重叠，全部场景无 `console.error` / `pageerror`。

## 2026-08-04 第八阶段：高保真 2.5D 战场表现闭环

- 第七阶段已提交：`6fc8e42 feat: 完善象棋竞赛规则与交互提示`。
- 将 `resources/art/production/redesign/` 当前红黑 `*_v3.jpg` 共 14 张逐项复制到 `public/assets/characters/`，逐文件二进制比对通过；运行时目录不含 `archive_*`、`misaligned` 或 `failed` 资源。
- `pieceVisuals.ts` 建立红黑全彩角色 manifest，统一颜色图、同源 Alpha、剪影回退、像素尺寸、脚底锚点与最大占位；角色卡按“几何占位 → 锁定剪影 → 全彩”逐级切换，任一贴图失败都不会只剩底座或阻塞棋局。
- 棋盘升级为金属基座、石质面层和金边框；棋盘外新增三层竞技台、四根残柱、六块悬浮岩、84 粒确定性尘埃及红蓝环境光，所有漂浮和呼吸节奏均由模拟时钟驱动。
- 选择、可走、可吃和将军提示继续贴合棋盘交点，并增加呼吸节奏、接触阴影和软阴影；楚河汉界及坐标文字已按红方视角校正。
- 吃子命中增加 `65ms` 打击停顿、18 枚火花、冲击波、瞬时点光和确定性镜头震动；既有车、马、炮与通用移动时长不变。
- 新增 Web Audio 原创合成音效，覆盖选择、普通落子、车冲锋、马跃、炮击、命中、将军和终局；仅在用户手势后解锁，不支持或被浏览器拒绝时静默降级。
- 战果 HUD 使用现有红黑 14 张 badge，按棋种合并数量并显示总吃子数与子力差；棋盘两侧同步摆放缩小的被俘角色。两处都只读取权威 `captured` 状态，因此悔棋、重开与棋谱回放会同步恢复。
- `render_game_to_text()` 增加角色资源、竞技场、战斗特效、侧边俘获与音频快照，可观察贴图就绪、回退实例、震动强度、待播 cue 和回放收敛状态。

### 第八阶段验收

- `npm test`：12 个测试文件、144 个测试全部通过；新增全彩角色规格与故障回退、音效事件／生命周期边界、HUD 战果延迟呈现，以及打击停顿确定性断言。
- `npm run typecheck`、`npm run build` 与 `git diff --check`：全部通过；主包 `624.06 kB`（gzip `165.30 kB`），仅保留 Three.js 单包超过 `500 kB` 的非阻断提示。
- Playwright 初局：14 / 14 张全彩角色与 7 / 7 张 Alpha 就绪，逻辑 / 渲染 `32 / 32`、回退实例 `0`；选中红马后金色选中圈和两个合法落点正常。
- Playwright 炮击：命中停顿期间火花、冲击波、点光、镜头震动与命中音效同步生效，黑马仍在棋盘时战果保持 `0`；演出结束后逻辑 / 渲染收敛为 `31 / 31`，黑马 badge 与侧边俘获角色在同一时点更新为 `1`。
- Playwright 悔棋恢复为 `ply=0`、逻辑 / 渲染 `32 / 32`、侧边俘获 `0`；吃子后棋谱回退再前进恢复为 `31 / 31`、侧边俘获 `1`，证明回放没有遗留或重复实例。
- 已人工检查初局、选择、命中、打击停顿、吃子完成、悔棋及回放恢复截图；桌面 `1280×720` 下无布局遮挡，全部有效场景无 `console.error` / `pageerror`。

## 2026-08-04 第九阶段：竖屏、触控与移动渲染适配

- 第八阶段已提交：`167b738 feat: 完成高保真 2.5D 战场表现`。
- 已扩展原始网页游戏 Playwright 客户端，仅增加可指定 viewport 的能力；仍沿用同一动作、手动时钟、截图、文本状态和错误收集流程。
- `375×667` 未适配基线确认：固定相机只更新 aspect，导致棋盘左右边线与底线棋子出屏；顶部回合／战果／状态覆盖黑方角色，底部两行控制覆盖红方底线。逻辑状态仍为 `32 / 32`，但竖屏关键交点不可可靠点击。
- 新增统一 `PresentationProfile`：桌面保持原 `42° / (0,11,-10)` 构图；手机、平板按 HUD 安全矩形 contain 棋盘，并统一控制 DPR、阴影、纹理档位、俘获陈列和角色卡姿态。`ResizeObserver`、动态 viewport、全屏与 BFCache 恢复都复用同一重排入口。
- 手机竖屏使用 512 贴图、DPR 上限 `1.25`、关闭实时阴影并保留接触阴影；平板使用 768、DPR `1.5`、1024 阴影；桌面继续使用 source 与 2048 阴影。切档采用 stable Texture + 独立 replacement，释放旧 GPU storage，并用 revision 阻止异步旧请求覆盖；后台失败继续显示旧档。
- 新增 `scripts/generate_runtime_character_assets.sh`，从 production 定稿生成并验证 512 / 768 两档共 42 张颜色／Alpha 资源；脚本检查数量、Alpha 与成对尺寸，并保留同档位下独立生成的 bases / badges。
- HUD 新增 compact portrait、tablet portrait 与 side-rail 横屏布局：手机顶部信息分区、底部 `3×2` 按钮、棋谱底 sheet 和安全区弹窗；`844×390` / `667×375` 改用左右侧栏，让棋盘保留中央区域。主要按钮、棋谱行和设置项均不低于 `44px`。
- 棋盘手势改为主指针 `pointerup` 提交；超过 `10 CSS px` 的拖动、多指、右键、非主指针与 `pointercancel` 都不会落子。全屏能力在运行时探测，不支持时隐藏按钮，失败时显示中文可行动提示。
- `render_game_to_text()` 增加 viewport、相机、棋盘投影、安全区、最小格距、纹理 requested / active 档及重载状态，便于自动化验证。

### 第九阶段验收

- `npm test`：14 个测试文件、177 个测试全部通过；新增手势、多视口 HUD、相机／画质档位、纹理 replacement 生命周期和阴影材质失效测试。
- `npm run typecheck`、`npm run build` 与 `git diff --check`：全部通过；主包 `644.91 kB`（gzip `170.31 kB`），仅保留既有的单包超过 `500 kB` 非阻断提示。
- Playwright `375×667`：棋盘外沿位于 `(15.5,151.2)–(359.5,523.9)`，落入 HUD 安全区，最小格距 `36.1px`；14 / 14 张角色、7 / 7 张 Alpha 使用 512 档就绪，手机隐藏 3D 侧边俘获陈列。
- 真实 touch 验证：红马 `(1,0)` 被选中后出现两个合法落点，继续点按完成 `(1,0) → (2,2)`，最终 `ply=1`、动画 `idle`、输入解锁；手机棋谱、设置与规则弹窗均正确锁盘／滚动且无布局遮挡。
- Playwright `390×844`、`768×1024`、`844×390` 与原始客户端 `1280×720` 均完成截图验收；竖屏棋盘全部 clear，窄横屏棋盘落入左右侧栏之间，桌面镜头与 source 纹理不回退，所有场景无 `console.error` / `pageerror`。
- 边界视口补充验收：`800×600` 顶部 HUD 预留 `160px`，棋盘顶边 `176.7px`，无状态条重叠；`568×320` 棋盘落入左右侧栏之间，六个操作按钮实测均为 `45×44px` 且工具栏不越界。模拟顶部 `47px`、底部 `34px` 设备安全区，以及横屏左侧单独 `44px` 刘海后，相机都会实时重算并保持棋盘 clear。
- 同页动态切档验证 `source → 512 → source`：每段 requested / active 档一致、14 + 7 张贴图全部 ready、无 reloading / failedReload 与浏览器错误；仅记录 SwiftShader 截图 `ReadPixels` 的已知性能提示。

## 2026-08-05 第十阶段：可旋转战场与观战朝向

- Canvas 新增鼠标与触控水平拖动环绕；相机只绕世界 Y 轴，保留各 `PresentationProfile` 的俯角、FOV、投影中心和用户 yaw。`pointerup` 点按走棋继续使用同一手势状态机，`10 CSS px` 死区、额外触点、取消与丢失 capture 均不会误走棋。
- 修正分段 PointerEvent 越过死区时的累计量：首次进入拖动会补齐起点总位移，旋转角度不再依赖浏览器事件频率；拖动结束不会遗留 `activePointerId` 或 grabbing 光标。
- 旋转安全取景使用当前设备 `0°` 基准棋盘矩形做边界并二分求最小距离，避免桌面 45° 棋盘压住底部工具栏；高频求解复用相机与投影向量，不在每个 pointermove 重复 clone。相机拉远时线性雾距按同一比例扩展，修复手机侧视棋盘被远雾吞没的问题。
- 阵营世界朝向固定为红方 `+Z`、黑方 `-Z`。默认红方视角显示红方背向剪影／黑方正面，转到黑方半场后互换；侧视临界区使用 `0.12` 滞回避免闪烁，被俘展示仍强制正面。背向层复用同源 Alpha 轮廓，不镜像正脸，也不计入资源加载 fallback。
- `board-typography` 与当前观战半场同步：红方半场保持 `rotation=180°`，黑方半场切到 `0°`，因此旋转 180° 后“楚河／汉界”和边缘坐标仍朝向当前用户；与角色翻面共用同一滞回阈值。
- HUD 常驻短提示改为“点按走棋 · 拖动旋转视角”。`render_game_to_text()` 增加 yaw、取景比例、拖动状态、相机位置、阵营 `front/back`、棋盘文字观战方与角度，便于自动验收。

### 第十阶段验收

- `npm test`：15 个测试文件、185 个测试全部通过；新增相机环绕／雾距、事件频率无关拖动、阵营正背面、侧视滞回和棋盘观战方断言。
- `npm run typecheck`、`npm run build` 与 `git diff --check`：全部通过；主包 `651.45 kB`（gzip `172.17 kB`），仅保留既有的单包超过 `500 kB` 非阻断提示。
- 原始网页游戏 Playwright 客户端复测：轻触红马仍得到 2 个合法落点，`livePly=0`、`fallbackInstances=0`、`backViewInstances=16`，无 `console.error` / `pageerror`。
- 桌面 `1280×720` 与真实 CDP touch 手机 `375×667`：0°、45°、180° 拖动均不改变 `ply`，棋盘完整且不碰 HUD；180° 时 `red=front / black=back`、文字 `viewerSide=black / rotation=0°`。手机 45° 使用 512 档、取景比例约 `1.46`，雾修复后棋盘与双方棋子均清晰可见。
- 最终状态与截图位于 `/Users/bril/Documents/Codex/2026-08-04/https-x-com-cellinlab-status-2084507607713116460/work/playwright-camera-orbit/final-fog-and-typography/`；额外 `390×844` 触控证据位于同任务的 `work/playwright-orbit-latest-review/`。
- 已录制 `1280×720` 真实指针演示：红黑视角往返后依次完成红炮吃黑马、黑车反吃红炮、红马跃进、黑卒／红兵推进及黑卒吃红兵；最终 `ply=6`、共吃 3 子、轮到红方，控制台无错误。约 60 秒成片与逐手状态报告位于同任务的 `work/recordings/xiangqi-operations-demo-final/`。

### 后续资源建议

- 当前 production turnaround 是带灰底／文字、无独立 Alpha 的建模参考，且黑方缺仕／卒两类，不能直接充当运行时背面。下一步若继续 2.5D，应完整输出红黑 14 张紧凑 `*_back_v3.jpg` 与 7 张同源 `sil_*_back_alpha.png`，并生成 512 / 768 档；若要求侧视连续自然，应直接推进多方向角色图或 GLB。

## 2026-08-06 第十一阶段：统一战斗演出时间线（借鉴 rork-medieval-3d-chess）

参考仓库：<https://github.com/alexngdev99/rork-medieval-3d-chess>。只借鉴其**战斗演出编排**，
不引入 React / chess.js / GLB / 重后处理，也不复制其 4778 行 SceneEngine 结构。

### 阶段 0：演出事件流

- `AnimationDirector.advance()` 由返回 `boolean` 改为返回 `{ completed, events }`。
- 事件表在 `start()` 时一次性排定，`advance()` 只发出区间 `(before, after]` 内穿过的标记，
  每个标记恰好发出一次。修复了原来「轮询 `phase` 是闩锁语义」的隐患：单帧跨多个阶段时
  （手动时钟快进、掉帧）不再丢失命中或收尾事件。
- `gameAudio` 的 `animation-phase` 信号替换为 `animation-event` / `animation-finished`，
  可观察行为（哪些 cue、什么顺序）保持不变。

### 阶段 1：BoardScene 四拆（零视觉变化）

- `boardScene.ts` 1968 → 724 行，新增 `boardGeometry` / `textureLibrary` /
  `cameraDirector` / `combatEffects` / `piecePresenter`。
- 相机改为「用户轨道 → 剧情构图 → 命中震动」三层合成。规则拾取与广告牌朝向只读轨道层。
- 验收标准是**零视觉变化**：185 测试全绿、`typecheck` / `build` 通过，
  Playwright `1280×720` 基线 32/32 实例、14/14 全彩、7/7 Alpha、回退 0、剧情层归零。

### 阶段 2：CombatProfile 与新节拍

- 新增纯数据 `combatProfile.ts`（不 import three，可在无 DOM 环境断言）：
  七棋种各自的蓄力、行进、命中、打击停顿、消散、占领、收势与强度。
- 时间线扩展为 `windup → travel|projectile → impact → victim-exit → occupy → settle`。
- 层次刻意拉开：帅将最慢最重（1-cell 吃子 1340ms），仕士与兵卒短促轻量（800–850ms），
  象的占领强度最高（重型落地），马腾空无落步，炮不走路只后坐。
- 单次吃子硬上限 `1.6s`，超限等比压缩；新增演出速度倍率与 `prefers-reduced-motion`
  （压时长与幅度，保留全部阶段）。

### 阶段 3：车／炮纵切与立绘消散

- 车：后拉蓄力 → 冲锋拖尾 → 重命中 → 进位压点（`lift` 下沉 `-0.03` 再回弹）。
- 炮：原地后坐蓄能 → 弹道 → 爆炸 → 目标消散 → **占领阶段才进位**；
  炮响从起手帧移到 `projectile-release`，占领闷响落在炮身真正走到落点的 77.5% 处。
- 被吃立绘改为自下而上噪声消散＋发光边缘（着色器内生成噪声，不引入额外贴图），
  全彩 / 剪影 / 描边三层共享同一 uniform，几何占位退化为整体淡出。
- 新增 `footstep` / `claim` 两个原创合成音效。
- 新增验收钩子 `window.projectSquare(file, rank)`，Playwright 不再硬编码像素坐标。

### 验收

- `npm test`：16 个测试文件、202 个测试全部通过（新增战斗档案 10 项、演出时间线 8 项）。
- `npm run typecheck`、`npm run build` 通过；主包 `655.87 kB`（gzip `173.49 kB`）。
- Playwright `1280×720` 真实点击验证炮吃马全程：
  时间线 `windup 175 / travel 494 / impact 190 / victim-exit 280 / occupy 210 / settle 90`，
  事件表 `windup@0 projectile-release@175 impact@669 victim-dissolve@859 claim@1301.75
  settle@1349 complete@1439`；消散中段 `0.636`、占领时 `1`；
  剧情镜头在命中段 `active`、收势前归零；最终逻辑/渲染收敛为 `31/31`。
- 车吃炮全程同样验证；**演出中途悔棋**回到 `ply=1`、`31/31`，
  被复活的红炮不透明（消散 uniform 已复位），战果与侧边俘获同步回退。
- 全部场景无 `console.error` / `pageerror`。

### 未做（有意）

- 不重写为 React / Radix UI，不替换规则、AI、棋谱与回放，不引入 GLB 骨骼系统。
- 不开放自由俯角：立绘是广告牌，放开俯角会暴露「卡片贴在平板上」，
  2.5D 的幻觉依赖锁定俯角。若要自由环视应直接推进多方向角色图或 GLB。
- 桌面点击死区保持 `10px`（未降到 `8px`）：触控板抖动比鼠标明显，
  死区过小的失败模式是「点了没反应」，比「棋盘转了几像素」更糟。
- WebGL context-loss 仅计划做检测＋中文提示，完整资源重建暂不实现。

## 2026-08-06 第十二阶段：拾取修复、七棋种差异化、战术／录制模式与画质档

### 棋子 collider 优先拾取（真 bug 修复）

- 此前 `pickSquare` 只射向 `y=0` 地面。立绘约一格多高，点在棋子身上会穿过去
  命中它**背后**的空交点。
- 每枚棋子新增不可见圆柱 collider：半径按 Alpha 可见宽度收窄并夹在半格内，
  高度取 `ROLE_BASE_TOP + visibleHeight`。命中且比地面更近时优先采纳。
- 刻意**不** raycast 立绘平面——Three 的 raycast 不做 alpha 测试，打在贴图透明
  角落会把邻近空交点的点击吞掉。
- 交点由 `sync()` 按权威局面写入 collider，不从 mesh 坐标反推（演出期间 mesh 在动）。
- 桌面 `1280×720` 实测：从黑车交点向上偏移 `0 / -30 / -55px` 全部命中黑车；
  修复前 `-55px` 会落到空交点。

### 七棋种差异化

- 新增蓄力预兆环：出手前在**起点**由外向内收紧，颜色取阵营色，强度直接由
  `CombatProfile.impactStrength` 驱动（`< 0.5` 完全不显示）。
- 于是帅将、车、炮起手时地面明显发沉，仕士与兵卒几乎看不见——差异化不需要
  为每个棋种单独写演出代码，档案数值就是差异本身。

### 将军两段式

- 原来是恒定呼吸脉冲，"刚被将"和"一直被将"读起来一样，且长期高亮盖住棋盘。
- 拆成宣告瞬间的强 flare（`620ms`，叠加 `+0.78` 不透明度与 `+0.62` 缩放）
  与随后的低亮 watch（基线 `0.16`）。
- flare 由模拟时钟驱动并以「行棋方 + 将的坐标」为键去重，因此悔棋与回放完全可复现。

### 战术俯视（T）与录制模式（C）

- 战术是**离散备用构图**，不是放开自由俯角——立绘是广告牌，连续俯角会暴露
  「卡片贴在平板上」。切换时同时隐藏立绘、只留底座汉字，因此俯视是安全的。
- 修掉一个连带 bug：桌面 `framingInsetsCss` 为 `0`（3/4 构图本来就避开 HUD），
  俯视沿用它会把棋盘底边推到 `y=720` 压住工具栏。新增
  `PresentationProfile.hudReservedCss`（始终含 HUD 预留 + 设备安全区），
  备用构图用它取景。修复后棋盘落在 `(384,80)–(896,644)`，安全框 `(16,76)–(1264,644)`。
- 录制模式收起全部面板，只留右下角一个淡出的「退出录制 C」按钮——
  不能让用户只能靠猜快捷键退出。

### 画质三档

- `QualityTier`（`high / balanced / lite`）是与 `resolvePresentationProfile`
  **正交**的预算覆盖层，逐项取较严者。刻意不把运行时状态混进那个纯函数，
  否则它就无法在无 DOM 环境下断言。
- 运行时降档带滞回与冷却，且**只降不升**：阈值附近来回自动升降会不断重建
  阴影贴图与 DPR，比停在低档更糟。
- 手动时钟下完全不采样，`> 100ms` 的帧间隔也不计入——否则一次
  `advanceTime(2000)` 会被读成 `0.5fps`，一步就把画质打到最低档，
  自动化验收随即失去可比性。
- DPR、阴影开关与阴影贴图尺寸统一走 `applyQuality()`，避免 profile 与画质档
  两条路径各写一半互相覆盖。

### 验收

- `npm test`：17 个测试文件、212 个测试全部通过（新增画质档 9 项、蓄力差异 1 项）。
- `npm run typecheck`、`npm run build`、`git diff --check` 通过。
- Playwright `1280×720`：collider 拾取三档偏移全部命中；战术模式 5 个探测点
  全部落在 canvas 上（修复前 `(1,0)` 命中「重开」按钮、`(4,0)` 命中工具栏），
  战术模式下正常完成 `(1,0) → (2,2)`；录制模式面板全隐、退出按钮可见并可恢复。
- 全部场景无 `console.error` / `pageerror`。

## 2026-08-07 第十三阶段：接入现成 VFX 资源

### 攻击姿态资源实况更正

`resources/art/production/ASSET_PHASES.md` 记录的「黑方攻击姿态未出」已过时。实测：

- **攻击姿态红黑各 7/7，已齐**（`poses/attack/` 与 `poses/black/`），且为 1024×1024，
  与运行时角色卡尺寸一致，锚点体系可直接复用。
- **唯一硬阻塞是这 14 张全部没有 Alpha 蒙版**。轮廓与待机差异极大——车的戟尖
  伸到 x≈980，而待机车 Alpha 边界只到 739，复用待机蒙版会把整把兵器切掉。
- 姿态图是平滑深蓝灰渐变底且角色不触边，可用四角 flood fill + 颜色距离容差
  自动派生 Alpha（比亮度阈值稳，不会吃掉黑甲黑靴）。尚未实施。
- 非阻塞缺口：死亡姿态 4/14、受击 2/14、行走 2/14。死亡帧目前由 dissolve
  着色器代偿，行走由步态与扬尘代偿。

### 接入的资源

| 资源 | 替换了什么 |
|------|-----------|
| `vfx_footstep_dust_alpha.png` | 新增——此前 `footfall` 事件**没有任何视觉** |
| `vfx_cannon_fireball_alpha.png` | 程序化球体 + 圆柱拖尾 |
| `vfx_chariot_slash_trail_alpha.png` | 程序化圆柱拖尾 |
| `vfx_ground_wave_ring_alpha.png` | 程序化 `RingGeometry` 占领环 |

### 实现要点

- `AnimationSurface` 新增 `onAnimationEvent(event)`：连续量走逐帧 `set*`，
  一次性瞬间（落步扬尘）走事件订阅。扬尘由**模拟时钟**自行衰减，
  因此悔棋、回放与手动时钟都得到一致画面。测试断言表现层订阅到的事件流
  与 `advance()` 返回值逐项相等。
- **火球贴图自带方向性拖尾**（头在右上约 45°），必须按屏幕空间飞行方向旋转：
  `material.rotation = atan2(dy, dx * aspect) - π/4`。sprite 四边形在世界里是
  正方形、投影后像素也是正方形，所以 NDC 的 x 乘 aspect 就是真实像素方向。
- **车刀光用 Alpha 作形状、阵营色作颜色**。原图是红金的，直接叠加会让黑方也
  拖出红光；乘一个蓝色又会把红金乘成黑色。只取轮廓才能既保住作者画好的形状，
  又保住阵营识别。
- 逐像素测量了 5 张 VFX 的 Alpha：`brightButTransparentPct` 全为 0，说明 Alpha
  与亮度完全吻合、是干净遮罩；但车刀光 `meanAlpha` 仅 `14.6/255`、
  `alpha>32` 只占 14%，是很淡的软遮罩，直接当形状用会发虚。因此在着色器里
  抬一档 gamma：`clamp(pow(a, 0.5) * 1.55, 0, 1)`。

### 验收

- `npm test` 212 全通过；`typecheck` / `build` / `git diff --check` 通过。
- Playwright `1280×720`：`/assets/` 零 4xx、零 `console.error`。
  车冲锋刀光尾迹方向正确（尾在车身后方）并呈阵营红；炮弹头朝飞行方向、
  尾焰朝后；占领地波在落点展开为金色能量环；扬尘随落步生成并按模拟时钟衰减。

### 未接入（有意）

- `board_albedo_topdown.jpg`：需先做 UV 对齐验证，规则交点与拾取平面仍须由
  程序控制，贸然贴图会与现有线条／楚河汉界层错位。
- `overlay_check_jiangjun.jpg` / `overlay_checkmate_juesha.jpg`：`1280×720` 无 Alpha，
  作全屏叠加需 additive 且只在 flare 期间短暂出现；屏幕空间四边形的接线成本
  高于收益，留作后续 DOM 覆盖层实现。
- `vfx_judgement_pillar_alpha.png` / `vfx_steel_slash_arc_alpha.png`：留给后续
  帅将／象的重型命中演出。

## 2026-08-07 第十四阶段：攻击姿态 Alpha 派生与接入

### 从 JPG 派生 Alpha（`scripts/derive_pose_alpha.mjs`）

14 张攻击姿态是深蓝灰渐变底的 JPG，零 Alpha，且轮廓与待机差异极大
（车的戟尖到 x≈980，待机车 Alpha 边界只到 739），不能复用待机蒙版。

**第一版用「颜色距离 + 区域生长」失败**：黑甲、黑靴、深战袍和车轮全被吃掉。
实测原因很明确——

| 采样点 | RGB | Sobel |
|---|---|---|
| 背景 | ~(30,32,34) | 0 |
| 黑靴 | (13,11,14) | 2 |

黑靴**比背景还暗**，色距仅约 34，靠颜色无论取什么阈值都分不开。

**第二版改用边缘屏障**：背景自身 Sobel 分位只有 `p99=7.1 / max=14`，而角色轮廓
远高于此。让填充「顺着平滑背景走、撞到边缘就停」——角色内部再暗也进不去，
因为它被自己的轮廓包住了。阈值 `EDGE_LIMIT=18`。

14 张全部合格：覆盖率 12–40%（无残留背景），红黑同棋种数值几乎一致
（车 19.94% vs 20.38%，边界差几像素），证明是同一渲染的重着色、派生稳定。

> 教训：**只看 bounds 通不过体检**。第一版漏抠后 bounds 几乎没变（极值由兵器
> 和红缨决定，它们没被吃），必须目检才发现内部空洞。

### 运行时接入

- `pieceVisuals.ts` 新增 `ATTACK_POSE_SPECS` / `getAttackPoseLayout`：
  平面尺寸沿用同棋种待机姿（同一套生成框，共用尺寸即保证人物大小一致；
  攻击姿显得矮是因为下盘拉开架势，这是对的）；横向锚点用**脚部条带中心**
  而非 bounds 中心——车的戟、炮的炮身会把 bounds 拉偏，用它切换时会横跳。
- 姿态贴图**首次真正用到时才创建图层并加载**。14 棋种 ×（彩图 + 蒙版）= 28 张，
  全塞进初始加载会让开局多等一大截，而一局里真正出手的通常只有几枚棋子。
  实测：初始加载 `0` 张；红炮出手后只加载它自己的 `2` 张。
- 窗口取「蓄力起 → 命中结束」，且**只有吃子才启用**（空着是行军，不该拔刀）；
  消散与占领回到待机。取消演出会显式复位。

### 已知限制

**红方在默认视角看不到自己的出手姿**（已补资源，见下方「背向攻击姿」）。
此前默认相机在红方一侧时红方 16 枚全是背向，出手姿只有正面图；按「背向绝不
镜像正脸」规则不启用。现已补 14 张背向攻击姿 + 蒙版并接入 `role-attack-body-back`。

### 验收

- `npm test` 213 全过；`typecheck` / `build` / `git diff --check` 通过。
- Playwright `1280×720`：黑车吃子全程 —— 蓄力/命中帧为张开架势的出手姿
  （红金兵器横在身前），消散后回到手持直枪的待机姿，底座无跳动；
  `/assets/` 零 4xx、零 `console.error`；最终 `30/30` 收敛。
- `public/assets/poses` 共 11MB（14 彩图 + 14 蒙版），因懒加载不进首屏。

## 2026-08-07 第十五阶段：角色辉光与环绕光点

两者**都不需要图片资源**，全部程序化生成——而且用图反而做不好。

### 轮廓辉光（`role-halo` 层）

- 对角色**自己的** Alpha 蒙版做 12 方向 × 2 圈半径共 24 次采样取平均，得到一张
  放大、模糊过的剪影。因此光晕严丝合缝贴着每个棋种各自的身形。
- **用一张通用光斑贴图做不到这件事**：七个棋种轮廓各不相同，通用光斑必然对不齐。
- 图层放大 `1.16`、压在本体之后（`renderOrder 3`，`z = -0.006`），加色混合。
- 强度由 uniform 驱动：常态是按棋子相位错开的缓慢呼吸（`0.34 ± 0.14`），
  出手时叠加 `+0.6` —— 直接复用既有的 `attackPose` 标志，不新增 surface 方法。
- 蒙版没就绪就不亮（复用既有的 `visibility.rim` 判据）。

### 环绕光点（`characterAura.ts`）

- 全场共用**一个** `THREE.Points`。32 枚棋子各带 10 个点，若每枚一个粒子系统
  就是 32 次 draw call；合并后只有 1 次。实测 `activeMotes: 320`。
- 光点纹理用 canvas 径向渐变现生成，比外部贴图更锐利且零加载成本。
- 运动完全由模拟时钟 + 逐棋子固定相位（id 派生的 FNV 哈希）决定，
  不读 wall clock、不用随机数——悔棋、回放与手动时钟画面一致。
- 螺旋上升 + 到顶淡出，颜色分阵营（红方暖金 `0xffcf6a`、黑方冷蓝 `0x8fd0ff`）。
- buffer 只增不减，避免每次吃子都重建。

### 画质档

`QualityBudget` 新增 `characterGlow` / `motesPerPiece`：
`high` 开辉光 + 10 点、`balanced` 开辉光 + 5 点、`lite` **两者全关**
（辉光是每棋子一个 draw call 且片元做多点采样，低档必须整体关掉）。
战术俯视下光点也关闭——立绘已隐藏，光点浮在扁平底座上没有意义。

### 验收

- `npm test` 214 全过；`typecheck` / `build` / `git diff --check` 通过。
- Playwright `1280×720`：黑方冷蓝、红方暖金光晕均贴合各自身形（可辨认枪尖、
  盔缨轮廓）；`aura: {glowEnabled:true, motesPerPiece:10, activeMotes:320}`；
  零 `console.error`。

### 可调项

强度集中在两处：`piecePresenter.update()` 里的呼吸基线与出手加成，
`characterAura.ts` 里的 `size` / `radius` / `MOTE_COLORS`。

## 2026-08-07 第十六阶段：上下环绕光点 + 坠落流星

### 需求

粒子要**绕着角色上下环绕**，另加一颗**从头顶坠到脚下、像流星一样烧尽**的大粒子。

### 实现

- `PointsMaterial` 的 `size` 是全局的，撑不起「流星头大尾细」，因此换成自定义
  `ShaderMaterial`：逐点 `aSize` + 逐点 `aColor`，仍然只有 **1 次 draw call**。
- **环绕**：水平绕身（`ORBIT_SPEED`）+ 垂直往复（`BOB_SPEED`），半径按
  `sin(π·heightT)` 在头顶与脚下收窄成球形包络——否则轨迹读起来是套了个圆筒。
  每颗点的往复相位错开，任意时刻散布在整个身高上。
- **流星**：周期 `4.2s`、坠落只占 `17%`（要短促才像流星），8 点拖尾沿同一轨迹取
  稍早位置，头大尾细。出现时瞬间亮起、贴近脚下才烧尽。
- 全部由模拟时钟 + id 派生相位驱动，各棋子流星互相错开；实测任意帧有 4–8 枚
  同时在坠（理论值 `17% × 32 ≈ 5.4`，吻合）。

### 三个真 bug（都是自己写的，靠数据而非肉眼定位）

1. **点尺寸量级错了**。`gl_PointSize = size × uScale / -mvz`，桌面下约
   `size × 30` 像素。按世界单位的直觉取 `0.065`，算出来只有 **2 像素**，
   肉眼根本看不见。改为 `MOTE_SIZE=0.38`（≈11px）、`COMET_HEAD_SIZE=0.62`（≈19px）。
2. **二次缓动让流星在顶上悬停**。`eased = trailT²` 配合过早的 `burn` 衰减，
   使它没走完全程就烧尽。改为 `trailT^1.25`，并把烧尽窗口挪到最后 26%。
   离线数值验证：0.71s 内头点从 `y=1.53` 坠到 `0.28`，走完整个身高。
3. **加色混合下亮度被平方**。`SrcAlpha × One` 下输出 `vec4(rgb·a, a)` 的实际
   贡献是 `rgb·a²`；改成 `vec4(rgb·a, 1.0)` 才是 `rgb·a`。

### 教训

拖尾间距先被我从 `0.052` 缩到 `0.02`，理由是「糊成一根竖条」——那是**基于尺寸
bug 的误判**。尺寸修对后拖尾反而被头部完全盖住，又调回 `0.045`。
排查顺序应该是先验证量级、再调手感。

### 验收

- `npm test` 216 全过；`typecheck` / `build` / `git diff --check` 通过。
- 新增可验收字段 `presentation.aura.cometsFalling`，让自动化能定位到「有流星的帧」，
  而不是靠碰运气截图。实测时间序列：`4,5,7,7,8,7,6,5,5,5,5,4,6,6,6,5,5,6,5,4,4,4,5,7`。
- `activeMotes: 576` = 32 × (10 环绕 + 8 流星点)，仍是 1 次 draw call。

### 可调项

`characterAura.ts` 顶部常量：`ORBIT_SPEED` / `BOB_SPEED` / `ORBIT_RADIUS` /
`MOTE_SIZE` / `COMET_PERIOD_S` / `COMET_FALL_RATIO` / `COMET_HEAD_SIZE`。

## 2026-08-07 第十七阶段：修复「辉光与光点运行一会就消失」

### 症状与根因

用户报告辉光和光点跑一会儿就没了。实测复现：

```
t(s)  tier      fps   降档次数  glow   motes
  20  high         -         0   true     10
  22  balanced  20.9         1   true      5
[xiangqi-3d] 帧率偏低（20.9 fps），画质降至 balanced
```

**这是设计错误，不是偶发**：我把刚做的两个视觉主体挂在了自动降档路径上，
而降档策略是**只降不升**的。于是性能治理器第一个杀掉的正好是用户要的东西——
掉到 `lite` 就 `characterGlow: false, motesPerPiece: 0`，**且永远回不来**。

更糟的是**辉光本身就是新增的主要开销**（32 个额外 draw call × 每片元 24 次
纹理采样），等于这个功能触发了对自己的移除。

### 三处修复

1. **辉光成本砍到 1/3**：12 方向 × 2 圈 = 24 次采样 → 8 方向单圈。放大 1.16 倍
   再模糊之后，两者观感差别很小。实测停留在 `high` 的时间从 22s 延长到 60s。
2. **最低档保留光点**：`lite.motesPerPiece` 由 `0` 改为 `4`。光点是全场共用的
   **一次** draw call，几乎不花钱；真正贵的是辉光，关它就够了。
3. **单向只降 → 非对称滞回**：降档看 `minimumFps`（45 → 38，棋类 40fps 完全够用），
   回升要求更高的 `recoveryFps: 56` **且**连续 `3` 个窗口达标 **且**冷却已过。
   这样既不会在临界点抖动，也不会因为一次偶然的负载尖峰**永久**拿掉画面。

### 顺带修掉的日志 bug

方向判断原本写成 `next.tier < previousTier`——字符串比较，`'balanced' < 'high'`
恒为 true。改用 `QUALITY_TIERS.indexOf` 比较序号。

### 验收

固定档位逐档实测：

| 档位 | 辉光 | 光点/棋子 | 总点数 |
|---|---|---|---|
| high | 开 | 10 | 576 |
| balanced | 开 | 5 | 416 |
| lite | 关 | 4 | 384 |

新增 `quality.upgrades` / `quality.recoveryStreak` 遥测字段。
`npm test` 218（AI 搜索测试在 load average > 40 时会因 CPU 争用超时，
单独跑 8/8 全过，非回归）。

## 2026-08-25 第十八阶段：修复背视资源在移动端全部 404

### 症状

桌面一切正常，手机／平板上背向一方仍是**纯色剪影**——正是第十七阶段之后
`3340466` 那个提交要消灭的东西。桌面看不出来，所以一直没被发现。

### 根因：接入了资源，但没接入它的衍生档

`resolvePresentationTextureUrl` 把两类 URL 映射到移动端衍生档：

```
/assets/characters/*.jpg
/assets/silhouettes/sil_*_alpha.png
```

新加的背视资源命名恰好**两条都命中**（`red_king_back_v3.jpg`、
`sil_red_king_back_alpha.png`），于是 tablet／phone 去请求
`/assets/runtime/{512,768}/...`——而生成脚本当时只产 14 正面彩图 + 7 共用蒙版，
背视一张都没有。结果：`failed` → `backIdleReady=false` → 退回 `role-fallback`。

不是「忘了跑脚本」，是**脚本本身覆盖不到**：它硬校验 `-ne 14 || -ne 7`，
就算把背视塞进来也会直接 `exit 1`。

### 三处修复

1. **生成脚本扩到 28 彩图 + 21 蒙版**。背视的缩放源必须取 `public/assets/`
   而不是 `resources/art/production/back_idle/`——后者是**洋红底交付原画**，
   抠图产物只落在 `public/` 下，从 resources 缩放会把洋红烤进移动端贴图。
   正面蒙版红黑共用一张、背视蒙版逐阵营各一张，因此配对校验要分开写。
2. **补 `pieceVisuals.test.ts` 的「运行时资源可解析性」**：把表现层可能请求的
   全部 URL × 三档（`source/768/512`）逐个对照 `public/` 下的真实文件。
   这条断言跨了三个文件（URL 在 pieceVisuals、换档在 presentationProfile、
   文件在 public/），单看任何一个都发现不了问题。
   用 `import.meta.glob` 的**键**当白名单，非 eager，不读文件也不需要 `@types/node`。
3. **文档补上「新增贴图必须重跑脚本」**（`resources/README.md`、
   `back_idle/README.md` 的接入清单原本缺这一步）。

### 验收

- `npm test` 224 全过（新增 6 条）；`typecheck` / `build` 通过。
- 负向验证：任意删掉一张衍生档，对应档位的用例立刻失败并列出缺失 URL。
- 衍生档体积：512 档 2.6M、768 档 5.5M（原来分别是 1.6M / 3.4M）。

### 教训

**「桌面能看到效果」不等于接入完成。** 只要资源命名落进衍生档正则，
就必须同步产出全部档位，否则失败模式是静默降级而不是报错——
恰恰是最难被发现的那一种。

## 2026-08-25 第十九阶段：规则引擎性能（行为零变化）

AI「挑战」档一手要算 245ms，而 8000 节点预算只够搜两层——不是搜索写得笨，
是**每个节点太贵**。逐项量过之后，贵的地方一个也不在意料之中的位置。

### 三个真正的开销

1. **`isInCheck` 把整棵着法树摊开来回答一个是非题**。原写法生成敌方**全部**
   伪合法着，再看有没有落在将上。它是整个规则层调用最频繁的原语——
   每个候选着都要问一次。改成从将的格子**反查**：沿四方向各走一趟拿前两个
   挡子（定车、帅、炮），再定点查马、象、士、兵的有限个来源格。
2. **合法性筛选每个候选着克隆 32 个棋子对象**。改成在同一张占位表上
   落子—判断—还原（make/unmake）。占位表只存引用、不读坐标，
   所以棋子对象直接挪格即可，不必复制。
3. **规则状态每步深克隆整份历史**。`nextPositionOccurrenceCount` 为了数一个数
   先克隆整份 `RuleState`（然后丢掉），`advanceRuleState` 再克隆一次帧数组、
   逐帧再克隆捉子表。三次深遍历，单步开销随棋局长度线性增长、整局呈平方。
   实际上**没有任何调用方会改它**，克隆纯属多余。

顺带：局面签名原本「拼 32 个二十来字符的串再排序」，改成按格编码——
顺序无关性由固定格序天然保证，排序整个省掉。捉子分析改走回调，
把「算出现次数」和「按次数决定要不要分析」合并成一次遍历，不再各算一遍。

### 数字（同机 A/B，`git stash` 前后连续测，规避负载抖动）

| 项目 | 优化前 | 优化后 | 加速 |
|---|---|---|---|
| `isInCheck` | 0.0262 ms | 0.0120 ms | 2.2× |
| `generateAllLegalMoves` | 0.9479 ms | 0.0680 ms | **13.9×** |
| `analyzeChases` | 4.9541 ms | 0.4830 ms | **10.3×** |
| `applyKnownLegalMove`（提交路径） | 5.5669 ms | 0.2556 ms | **21.8×** |
| 走满 120 手（含每步捉子分析） | 391 ms | 52 ms | **7.5×** |
| AI 入门 | 5.93 ms | 1.55 ms | 3.8× |
| AI 标准 | 39.42 ms | 3.00 ms | **13.1×** |
| AI 挑战 | 245.02 ms | 31.75 ms | **7.7×** |

### 怎么保证行为没变

性能改动最怕「快了但悄悄错了」，尤其马腿、象眼、炮架这些判据。
因此先立等价性地基，再动手：

- `attacks.test.ts` 拿**未被改动的** `generatePseudoLegalMoves` 当参照，
  在 180+ 个实战局面与 120 个散布局面上，对 **90 格 × 红黑两方**逐格比对攻击判定。
- 合法着法整体与「克隆整副棋子再判将」的旧写法**逐条比对**
  （330+ 局面 × 两方，比 id、起点、落点、被吃子）。
- 散布局面专挑几何边界：士出宫、象过河、兵倒退、器械贴边。
  这类摆法实战不出现，但悔棋分支、AI 叶节点、捉子分析里的假想吃子都会走到。
- 另跑 60 局随机自对弈：`inCheck` 与重算一致、终局三元组自洽，
  五种终局原因全部触发；三档 AI × 120 手，候选着 100% 通过权威校验。

原有 224 项测试一条未改全过（新增 6 条，共 230）。

### 一处刻意保留的语义

传给捉子分析回调的出现次数是**未经吃子重置**的。搜索层据此判断这一着是否
逼近三次同形，这个判断与「吃子清空循环窗口」无关——两者混同会让 AI 在
吃子着上少算一次捉子分析，与优化前的行为不符。

### 量过但没改的

时间线每帧深拷贝规则状态，开销随手数线性增长；实测 100 手时每帧 0.073ms，
只占 16.67ms 帧预算的 0.4%。去掉防御性拷贝换来的收益不抵别名风险，不动。
