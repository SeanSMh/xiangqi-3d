Original prompt: 那你设计开发方案，开始开发吧，合理利用资源

## 当前里程碑

- [x] 完整基础象棋规则引擎与测试
- [x] 本地双人点选、合法落点、走棋与吃子
- [x] 将军、绝杀、困毙与重开
- [x] `render_game_to_text` / `advanceTime` / 全屏验收钩子
- [x] production v3 红黑 14 张全彩角色、Alpha 合成、剪影回退与角色 billboard
- [x] 坐标棋谱、单步悔棋、逐手定位与确定性自动回放
- [x] Playwright 可玩验证与截图检查
- [x] `program-competition-2023` 三次同形、长将／长捉、自然限着与死局自动裁决
- [x] 结构化非法着原因、统一中文提示与内置规则说明

## 约束

- 规则状态是唯一真相，Three.js 仅负责呈现。
- 红黑分别使用 production v3 定稿角色卡，并以同源 Alpha 裁切；颜色失败回退到锁定剪影，Alpha 未就绪或失败时使用几何占位，后续可按棋种逐步替换为 GLB。
- 美术资源只从 `public/assets/` 读取，禁止使用 `archive_*`。

## 2026-08-04 首个可玩里程碑

- `src/engine/moves.ts` 已实现帅/将、仕/士、相/象、马、车、炮、兵/卒，包含九宫、河界、象眼、马腿、炮架、将帅照面、自将过滤、将军、绝杀和困毙判负。
- `src/game/controller.ts` 负责选择、切换选择、合法着、走棋、吃子与重开；规则状态保持为唯一真相。
- `src/scene/boardScene.ts` 已接入 production 红黑底座 PNG，以及金色选择圈、白色合法圈、红色吃子圈；补全楚河断线和双方九宫线。
- HUD 已显示行棋方、将军、战果、终局、选择提示，并提供“重开 R”和“全屏 F”。
- 浏览器验收钩子已提供：`window.render_game_to_text()` 与 `window.advanceTime(ms)`。

## 验收记录

- `npm test`：2 个测试文件、19 个测试全部通过。
- `npm run typecheck`：通过。
- `npm run build`：通过，Vite 生产包已生成。
- Playwright：验证初始局面、红兵选择与合法圈、红黑各一步、红色吃子圈、兵吃卒、重开、全屏；最终吃子状态为 `ply=3`、`captured=["black:卒"]`，无控制台错误。

## 下一阶段 TODO

- [x] 增加 `AnimationDirector`：规则先结算，表现层锁输入并播放移动/受击/退场，完成后 `snapTo(nextState)`。
- [x] 完成车直线移动、阵营拖尾与通用吃子退场垂直切片。
- [x] 实现炮弹道和马日字跃迁，补齐三类演出差异。
- [x] 把 production v3 共用棋种剪影接到底座上；红黑共用造型，仅切换材质与阵营色。
- [x] 增加坐标棋谱、单步悔棋、逐手 / 自动回放，并支持悔棋后改走新分支。
- [x] 以红黑 14 张 production v3 定稿替换主视图剪影，并保留锁定剪影作为加载失败回退。
- [ ] 按棋种逐步试点 GLB 角色；继续遵守 `LOCKED_FEATURES.md`，不回退到错位或归档资源。
- [x] 增加玩家执红、AI 执黑的三级人机模式，并让 AI 搜索携带循环与限着上下文。
- [x] 完成第三次同形、长将／长捉责任比较、自然限着和仅剩将士象和棋。
- [x] 完成统一提示解析、具体非法着解释、第二次同形／自然限着预警和规则说明弹窗。
- [ ] 完成竖屏相机适配与窄屏专项截图验收。
- [ ] 竖屏阶段同步提供 512 / 768 运行时纹理与阴影质量分级，降低高分辨率角色卡在移动设备上的显存和阴影开销。

## 2026-08-04 第二阶段：走子与吃子演出

- 第一阶段已提交：`bd18e37 feat: 完成中国象棋本地双人可玩基线`。
- 新增 `src/animation/animationDirector.ts`，使用确定性 `travel → impact → victim-exit → settle` 状态机；权威局面先提交，动画期间只锁表现层输入。
- `BoardScene` 新增棋子浮点姿态、车阵营色拖尾、白青冲击核、橙金爆炸、受害者缩小抬升旋转，以及结束后的权威局面吸附。
- `window.advanceTime(ms)` 现在真实推进动画；`render_game_to_text()` 增加 `inputLocked`、`manualClock` 和完整动画快照。
- 重开可在任意动画阶段执行 `cancelAndSnap`；被吃 Mesh 删除时释放 geometry/material，缓存 Texture 保留复用。
- 修复过期 `Piece` 引用可能按错误坐标生成着法的问题，并补齐帅受攻击、被将应对、炮架将军和马腿将军测试。

### 第二阶段验收

- `npm test`：3 个测试文件、27 个测试全部通过。
- Playwright 半程：红车 `(0,0) → (0,1)` 在 `elapsedMs=183.333` 时视觉位置 `rank=0.568`，`inputLocked=true`；动画期间点击黑炮未产生新着法。
- Playwright 吃子：5 ply 合法序列触发红车吃黑炮，已截取 `impact` 与 `victim-exit`；最终 `captured=["red:马","black:炮"]`、动画回到 `idle`。
- Playwright 动画中重开：恢复 `ply=0`、32 子、`inputLocked=false`；全部场景无控制台错误。

## 2026-08-04 第三阶段：炮弹道与马跃迁

- 第二阶段已提交：`79bf8b9 feat: 增加走子与吃子演出状态机`。
- `AnimationDirector` 新增 `standard / chariot / horse / cannon` 演出类型；状态快照增加 `style` 与可观测的 `projectile` 姿态。
- 马使用固定 `480ms` 的日字贝塞尔路线：以马腿方向的相邻格作为控制点，并按曲线切线转向；半程最高抬升 `0.48` 格距。
- 炮吃子时炮身留在起点发射，炮弹以确定性抛物线飞向目标；命中后复用白青冲击与橙金爆炸，炮身在受害者退场阶段滑入目标格。
- `BoardScene` 新增炮弹亮核、橙色外焰和锥形短尾迹；结束或重开统一由 `clearTransientEffects()` 清理。
- 红黑双方共用棋种逻辑与特效参数，不依赖阵营专属贴图。

### 第三阶段验收

- `npm test`：3 个测试文件、29 个测试全部通过；新增马分步确定性、炮弹半程、命中和炮身入位断言。
- `npm run typecheck`：通过。
- `npm run build`：通过；仅保留 Three.js 单包超过 `500 kB` 的非阻断提示。
- Playwright 马跃半程：`elapsedMs=250`，视觉位置 `(1.271, 1.042)`、抬升 `0.479`、切线朝向 `0.48rad`；最终落在 `(2,2)` 并回到 `idle`。
- Playwright 炮弹半程：`elapsedMs=266.667`，炮身仍在 `(1,2)`，炮弹进度 `0.498`、位置 `(1,5.489)`、抬升 `1.25`。
- Playwright 炮击退场：`elapsedMs=833.333`，炮身已滑至 `rank=5.367`，受害者缩至 `0.256`，橙金爆炸生效。
- Playwright 最终局面：红炮完成 `(1,2) × (1,9)`，`captured=["black:马"]`、动画回到 `idle`；所有场景无控制台错误。

## 2026-08-04 第四阶段：production v3 角色剪影接入

- 第三阶段已提交：`7ceefd6 feat: 增加炮弹道与马跃迁演出`。
- 已核对 `LOCKED_FEATURES.md` 与 production/public 校验和：运行时只使用 7 张 `sil_*_alpha.png`；14 张红黑 JPG 保留为定稿与建模参考，全部 `archive_*` 继续禁用。
- 剪影 PNG 的 RGB 为纯黑、轮廓位于 Alpha 通道，因此不能直接依赖 `map × color`；`BoardScene` 改由材质 Shader 读取 `texture.a` 输出阵营色。
- 新增 `src/scene/pieceVisuals.ts`：集中保存七棋种资源 URL、Alpha 边界、落脚锚点和目标身高；可见横向占位全部不超过 `0.85` 格。
- 红黑共用同一棋种形状：红方暗红主体 / 金边，黑方玄青主体 / 冷蓝边；竖直角色卡采用 Y 轴 billboard，脚底锚定现有文字底座。
- `render_game_to_text()` 新增 `presentation` 摘要，可观察逻辑存活数、实际渲染实例、七类资源加载/失败状态。

### 第四阶段验收

- `npm test`：4 个测试文件、32 个测试全部通过。
- `npm run typecheck`：通过。
- `npm run build`：通过；仅保留 Three.js 单包超过 `500 kB` 的非阻断提示。
- Playwright 初局：32 个逻辑棋子对应 32 个渲染实例，红黑各 16，七类剪影全部 `ready`，无失败资源。
- Playwright 选中与马跃：选中圈、合法落点和角色卡均正常；马跃半程角色与底座同步沿日字轨迹移动。
- Playwright 炮击：炮弹飞行和受害者退场期间，权威局面已为 31 子、表现层仍保留 32 个实例；演出结束后收敛为 31 / 31，黑方 15 子。
- Playwright 演出中重开：立即恢复 `ply=0`、逻辑 / 渲染 32 / 32、动画 `idle`；全部场景无控制台错误。

## 2026-08-04 第五阶段：棋谱、悔棋与回放基础

- 第四阶段已提交：`452687a feat: 接入 production v3 棋子剪影`。
- 新增 `GameTimeline`：每个已提交着法保存稳定局面快照，实时分支与只读回放游标分离；悔棋只移除最后一个实时快照，不手写棋子逆操作。
- `GameController` 已提供逐手回放、任意已知 ply 定位、返回实时、单步悔棋及结构化坐标棋谱；显示文本使用 1-based 坐标，回放状态禁止棋盘继续落子并清空旧选择。
- 新增桌面棋谱抽屉与底栏控制：开局、上一步、播放 / 暂停、下一步、返回当前；`U / H / ArrowLeft / ArrowRight / Home / End / Space / Enter` 提供对应快捷键。
- 自动回放复用 `advanceSimulation()`，固定 `700ms / ply`，因此 `window.advanceTime()` 可确定性推进，不引入独立计时器。

### 第五阶段验收

- `npm test`：5 个测试文件、38 个测试全部通过，已覆盖普通着、吃子复活、绝杀恢复、回放只读、外部修改隔离和基线重置。
- `npm run typecheck`：通过。
- `npm run build`：通过；JS 产物 `528.76 kB`，仅保留单包超过 `500 kB` 的非阻断提示。
- Playwright 已验证：两步棋谱、回放锁盘、返回实时、悔棋后替代分支、自动回放 / 暂停及播放中重开；普通移动、炮弹飞行、受害者退场和吃子完成后悔棋均正确恢复。
- 吃子完成时为逻辑 / 渲染 `31 / 31`，悔棋后恢复 `32 / 32`；目前所有第五阶段场景均无控制台错误。
- 吃子棋谱回退到开局时恢复 `32 / 32`，再前进一步重新收敛为实时 `31 / 31`。
- 本阶段浏览器验收视口为 `1280×720`；窄屏样式已设断点，但竖屏相机适配与专项截图留到后续响应式阶段。

## 2026-08-04 第六阶段：AI 对弈与难度分级

- 第五阶段已提交：`1e99545 feat: 增加棋谱悔棋与确定性回放`。
- 保留本地双人，并新增“玩家执红、AI 执黑”的入门 / 标准 / 挑战三级人机模式；原生对局设置弹窗只在“应用并重开”后切换配置。
- AI 搜索运行在 Vite module Worker：搜索树使用 `generateAllLegalMoves()`、短路终局探测与轻量局面转换，最终候选仍经 `GameController → GameTimeline → applyMove()` 权威二次校验。
- 入门难度从固定种子的前四候选中选择，标准难度做一层稳定评估，挑战难度预判对方一手并受 `8,000` 节点上限约束；相同局面、难度与种子得到相同结果。
- `GameTimeline` 新增单调 `revision`；AI 结果必须同时匹配请求 ID 与 revision。悔棋、重开、回放、模式切换或替代分支都会终止 Worker，陈旧结果不能落盘。
- 人机悔棋原子回到玩家决策点：AI 思考时撤玩家 1 手，AI 已回复或动画中撤完整 2 手；AI 落子继续复用现有炮弹、马跃、移动与吃子演出。

### 第六阶段验收

- `npm test`：7 个测试文件、52 个测试全部通过；覆盖三级 AI 确定性、合法性、被将应对、两层战术避险、一步绝杀、困毙、节点预算与整轮降级、Worker 失败恢复 / revision 失效和整回合悔棋。
- `npm run typecheck` 与 `npm run build`：通过；Worker 独立产物 `7.00 kB`，主包 `540.73 kB`，仅保留超过 `500 kB` 的非阻断提示。
- Playwright 已验证：设置弹窗、三档完整回合、玩家走子 → AI 思考 → AI 吃子动画 → 返回玩家、思考中锁盘、思考 / 动画 / 回复后悔棋、AI 棋谱标识、回放期忽略悔棋且不插入 AI，以及切回本地双人。
- 入门难度完整回合为 `ply=2`、逻辑 / 渲染 `32 / 32`；标准难度完整回合为 `ply=2`、逻辑 / 渲染 `31 / 31`；挑战难度开局响应完成深度 2、`2,008` 个搜索节点。动画中与 AI 回复后的原子悔棋均恢复 `ply=0`、`32 / 32`，并清空 AI 决策；所有有效第六阶段场景均无控制台错误。
- 本阶段继续以桌面 `1280×720` 为完整视觉验收视口；竞赛循环规则已在下一阶段补齐。

## 2026-08-04 第七阶段：程序棋规与统一提示

- 新增 `RULES_AND_PROMPTS.md`，锁定 `program-competition-2023` 自动裁判档案，并明确与中国象棋协会 2020 版线下人工裁判规则的边界。
- 新增纯逻辑裁定层：局面键忽略棋子 `id` 和数组顺序但包含行棋方；同一局面第三次出现时比较长将 `2`、长捉 `1`、允许循环 `0`，等级高的一方判负，等级相同判和。
- 长捉使用合法吃子与逃捉关系，处理反吃保护、同类互捉、未过河兵、帅兵排除、马炮捉车和士象捉马炮车等程序棋规边界，不把普通攻击图直接当成长捉。
- 新增 120 个有效未吃子着自然限着：双方各自第 11 次及之后的将军与直接应将不计，吃子后循环窗口、有效着数和将军计数全部清零。
- 将死／困毙优先于循环，循环优先于自然限着，自然限着优先于仅剩将士象；所有结果和规则轨迹都进入 `GameTimeline`，可悔棋、可回放、可重开。
- AI 搜索复用权威规则转换并携带规则历史，能够正确评分规则和棋／判负，且不会选择立即令己方长捉判负的第三次同形着。
- 新增 `explainIllegalMove()`，可区分选错阵营、马腿、象眼、过河、九宫、阻挡、炮架、兵卒方向、未应将、送将和将帅照面。
- 新增 `deriveGamePrompt()` 作为 HUD 与 `render_game_to_text()` 的共同提示源；覆盖终局、AI、回放、弹窗、循环／限着预警和 21 类交互原因，并提供 `?` 规则说明入口。

### 第七阶段逻辑与构建验收

- `npm test`：10 个测试文件、127 个测试全部通过。
- `npm run typecheck`：通过。
- `npm run build`：通过；Worker `12.66 kB`，主包 `561.08 kB`（gzip `147.12 kB`），仅保留既有的单包超过 `500 kB` 非阻断提示。
- Playwright 真实点击已验证：规则说明打开时 `inputLockedReason=rule-help`、棋盘只读，关闭后恢复；点黑方棋子显示 `wrong-side`；红马点击被占马腿方向显示 `horse-leg-blocked`；提示在 `1800ms` 后恢复正常；合法走子进入 `ply=1` 且自然限着计数为 `1`。
- 已人工检查规则说明、选错阵营、蹩马腿、提示恢复、合法走子与关闭弹窗 6 张截图；布局和文案无重叠，全部场景无 `console.error` / `pageerror`。

## 2026-08-04 第八阶段：高保真 2.5D 战场表现闭环

- 第七阶段已提交：`6fc8e42 feat: 完善象棋竞赛规则与交互提示`。
- 将 `resources/art/production/redesign/` 当前红黑 `*_v3.jpg` 共 14 张逐项复制到 `public/assets/characters/`，逐文件二进制比对通过；运行时目录不含 `archive_*`、`misaligned` 或 `failed` 资源。
- `pieceVisuals.ts` 建立红黑全彩角色 manifest，统一颜色图、同源 Alpha、剪影回退、像素尺寸、脚底锚点与最大占位；角色卡按“几何占位 → 锁定剪影 → 全彩”逐级切换，任一贴图失败都不会只剩底座或阻塞棋局。
- 棋盘升级为金属基座、石质面层和金边框；棋盘外新增三层竞技台、四根残柱、六块悬浮岩、84 粒确定性尘埃及红蓝环境光，所有漂浮和呼吸节奏均由模拟时钟驱动。
- 选择、可走、可吃和将军提示继续贴合棋盘交点，并增加呼吸节奏、接触阴影和软阴影；楚河汉界及坐标文字已按红方视角校正。
- 吃子命中增加 `65ms` 打击停顿、18 枚火花、冲击波、瞬时点光和确定性镜头震动；既有车、马、炮与通用移动时长不变。
- 新增 Web Audio 原创合成音效，覆盖选择、普通落子、车冲锋、马跃、炮击、命中、将军和终局；仅在用户手势后解锁，不支持或被浏览器拒绝时静默降级。
- 战果 HUD 使用现有红黑 14 张 badge，按棋种合并数量并显示总吃子数与子力差；棋盘两侧同步摆放缩小的被俘角色。两处都只读取权威 `captured` 状态，因此悔棋、重开与棋谱回放会同步恢复。
- `render_game_to_text()` 增加角色资源、竞技场、战斗特效、侧边俘获与音频快照，可观察贴图就绪、回退实例、震动强度、待播 cue 和回放收敛状态。

### 第八阶段验收

- `npm test`：12 个测试文件、144 个测试全部通过；新增全彩角色规格与故障回退、音效事件／生命周期边界、HUD 战果延迟呈现，以及打击停顿确定性断言。
- `npm run typecheck`、`npm run build` 与 `git diff --check`：全部通过；主包 `624.06 kB`（gzip `165.30 kB`），仅保留 Three.js 单包超过 `500 kB` 的非阻断提示。
- Playwright 初局：14 / 14 张全彩角色与 7 / 7 张 Alpha 就绪，逻辑 / 渲染 `32 / 32`、回退实例 `0`；选中红马后金色选中圈和两个合法落点正常。
- Playwright 炮击：命中停顿期间火花、冲击波、点光、镜头震动与命中音效同步生效，黑马仍在棋盘时战果保持 `0`；演出结束后逻辑 / 渲染收敛为 `31 / 31`，黑马 badge 与侧边俘获角色在同一时点更新为 `1`。
- Playwright 悔棋恢复为 `ply=0`、逻辑 / 渲染 `32 / 32`、侧边俘获 `0`；吃子后棋谱回退再前进恢复为 `31 / 31`、侧边俘获 `1`，证明回放没有遗留或重复实例。
- 已人工检查初局、选择、命中、打击停顿、吃子完成、悔棋及回放恢复截图；桌面 `1280×720` 下无布局遮挡，全部有效场景无 `console.error` / `pageerror`。
